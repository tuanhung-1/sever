# Health Firmware

ESP32 firmware for the health monitoring and fall detection device.

## Local Credentials

Copy `secrets.example.h` to `secrets.h`, then fill in your WiFi and MQTT values. `secrets.h` is ignored by Git and should not be committed.

## Branch Scope

Use the `iot` branch for firmware work and keep commits limited to this `health/` folder.
